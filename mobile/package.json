{
  "name": "tapkasa-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Tapkasa — nativní obal (Capacitor) pro pokladní web z kava/index.html",
  "scripts": {
    "sync-web": "./sync-web.sh",
    "sync": "./sync-web.sh && npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@revenuecat/purchases-capacitor": "^13.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^7.0.2"
  }
}
